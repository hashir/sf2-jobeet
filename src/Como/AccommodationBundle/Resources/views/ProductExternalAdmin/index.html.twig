<h1>ProductExternal list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Txa_attribute</th>
            <th>Provider_short_name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('productexternal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.txaattribute }}</td>
            <td>{{ entity.providershortname }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('productexternal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('productexternal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('productexternal_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
