{% extends 'ComoTneBundle::layout.html.twig' %}

{% block headline %}Category Edit{% endblock headline %}

{% block content %}

<form action="{{ path('category_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }} class="well">
    {{ form_widget(edit_form) }}
    <legend></legend>
              <button type="submit" class="btn  btn-inverse">Update</button>
</form>

<!--ul class="record_actions">
    <li>
        <a href="{{ path('category') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('category_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul--->
{% endblock content %}